services:
  bambu-web:
    build: .
    ports:
      - "8080:5000"
    environment:
      - BAMBU_IP=${BAMBU_IP}
      - BAMBU_SERIAL=${BAMBU_SERIAL}
      - BAMBU_ACCESS_CODE=${BAMBU_ACCESS_CODE}
    restart: unless-stopped
